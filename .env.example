# Customer 360 Analytics Platform
# No external API keys required -- all data is synthetically generated.

# Optional: Override the default config path
# CONFIG_PATH=configs/config.yaml

# Optional: Override the default database path
# DATABASE_PATH=data/customer360.duckdb
